# Expected — private answer key. Do NOT publish to Cloudflare Pages.

- **Checkpoint 1**: It will run to completion but crash (or, depending on how `run_experiment`'s error isolation handles evaluator exceptions, silently produce zero `judge_correctness` evaluations) — because `judge_score()` is `raise NotImplementedError(...)` until filled in. The task function (`make_kiro_task`) is fully GIVEN, so Kiro CLI calls themselves succeed; only scoring is missing. This is why `scripts/compare_report.py` explicitly checks for empty score lists and tells the learner to go implement `judge_score()` rather than raising a raw `StatisticsError`.

- **Checkpoint 2**: Inconsistently, unless the rubric explicitly special-cases `AMBIGUOUS_BY_DESIGN:`. A rubric that just asks "does this match the expected answer 1-5" has no stable target for item-07 because `expected_output` is not a real answer — it's a flag. A well-built `judge_score()` detects the `AMBIGUOUS_BY_DESIGN:` prefix and scores the *answer's handling of ambiguity* (does it hedge appropriately / ask for missing policy context) rather than correctness-by-match. Reasonable full-credit solutions differ in phrasing but should score this item on a different axis than the other 7 — the acceptance criteria exempt item-07 from the "6 of 8 improve" bar precisely because its "correct" score depends on rubric design choices the lab intentionally leaves open.

- **Checkpoint 3**: Kiro CLI silently does nothing — a hook with an unrecognized trigger name is simply never matched; there is no validation step that flags a typo'd or non-existent trigger name at install time (per the hooks docs, the fields are just `name`/`trigger`/`matcher`/`action`, no enum validation surfaced to the user). This is exactly what makes Failure case 2 realistic: the hook file is syntactically valid JSON, `kiro-cli` accepts it without complaint, and the only symptom is *absence* of expected log lines — a data-shaped bug, not an error-shaped one. The fix is changing `"trigger": "PostStop"` to `"trigger": "Stop"` in `.kiro/hooks/log-usage-on-stop.json`.

- **Checkpoint 4**: Expected general trend (verify against your own run — model behavior/pricing may have moved since scaffold time): `haiku-4.5` (~0.4x cost) should be competitive-to-equal with `auto` (1.0x) on this task, because these are short, factual, policy-lookup-style questions with little need for deep reasoning — the "best score per credit" answer is usually the cheap model here, not `sonnet-5` (~1.3x). If `sonnet-5` doesn't meaningfully outscore `haiku-4.5` on this dataset, that itself is the finding: paying more did not buy a better answer for this task shape. Multipliers are documented at https://kiro.dev/docs/models/ as of scaffold time; do not treat the exact multiplier numbers as fixed.

- **Checkpoint 5**: No. The hook ships with the `PostStop` bug (Checkpoint 3), so the `improved` run's `credits_logged` will be 0 while `baseline`'s (if you happened to log a usage snapshot manually via `scripts/setup.sh` step 6, or ran baseline after already fixing the hook) may be nonzero — triggering the explicit `FAIL` branch in `compare_report.py`. The first successful, exit-0 run happens only after the learner fixes the trigger name and re-runs the `improved` variant.

## Final result

A learner who (a) implements `judge_score()` with an explicit ambiguous-item branch, (b) notices and fixes the `PostStop` → `Stop` hook bug, and (c) re-runs both variants should see: `compare_report.py` exits 0, improved's mean judge score exceeds baseline's on ≥6/8 items, both runs show nonzero `credits_logged`, and `model_compare.py` shows the cheaper model roughly matching the more expensive one on this task shape.
